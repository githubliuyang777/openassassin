apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: ops
type: Opaque
stringData:
  # 生产环境建议用 SealedSecrets 或 External Secrets Operator 管理
  JWT_SECRET: "<openssl rand -hex 32>"
  MASTER_KEY: "<openssl rand -hex 32>"
  ADMIN_DEFAULT_PASSWORD: "<强随机密码>"
